{
  "name": "gothic-lock-solver",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "solve": "node solve-lock.mjs",
    "benchmark": "node scripts/benchmark.mjs",
    "build:browser": "node scripts/build-browser.mjs",
    "verify:browser-bundle": "node scripts/build-browser.mjs --check",
    "verify:web-core": "node --experimental-vm-modules scripts/verify-web-core.mjs"
  },
  "bin": {
    "gothic-lock-solver": "./solve-lock.mjs"
  }
}
